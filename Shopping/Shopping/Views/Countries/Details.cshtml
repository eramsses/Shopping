@model Shopping.Data.Entities.Country

@{
    ViewData["Title"] = "FS | Detalles";
}

<h1>Detalles</h1>

<div>
    <h4>País</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div class="mt-2">
    <a asp-action="Index" class="btn btn-primary"><i class="fal fa-rotate-left"></i> Regresar</a>
    <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-outline-warning"><i class="fal fa-pencil"></i> Modificar</a>
    
</div>
